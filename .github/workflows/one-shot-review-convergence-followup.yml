name: One-shot review convergence follow-up

on:
  push:
    branches:
      - feat/review-dag-causal-freshness-convergence

permissions:
  contents: write

jobs:
  apply-verify-materialize:
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.ref_name }}
          fetch-depth: 0

      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Apply progress-sensitive causal Review DAG follow-up
        run: node scripts/one-shot-review-convergence-followup.mjs

      - name: Regenerate CLI references
        run: npm run docs:cli

      - name: Typecheck
        run: npm run typecheck

      - name: Focused causal review tests
        run: >-
          node --test --test-concurrency=2
          test/review-causal-dag.test.js
          test/review-causal-integration.test.js
          test/review-inspection-first.test.js

      - name: Full unit suite
        run: npm test

      - name: TypeScript E2E
        run: npm run test:e2e:ts

      - name: E2E product execution lint
        run: npm run lint:e2e-product-execution

      - name: Public documentation build
        run: npm run docs:build

      - name: Package dry run
        run: npm run pack:dry-run

      - name: Diff hygiene
        run: git diff --check

      - name: Commit only normal reviewable implementation files
        run: |
          rm -f scripts/one-shot-review-convergence-followup.mjs
          rm -f scripts/.one-shot-review-convergence-followup.payload
          rm -f .github/workflows/one-shot-review-convergence-followup.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: make causal review convergence progress-sensitive"
          git push origin HEAD:${{ github.ref_name }}
