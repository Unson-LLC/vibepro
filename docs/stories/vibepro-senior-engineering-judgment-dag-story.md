---
story_id: story-vibepro-senior-engineering-judgment-dag
title: 佐藤の思考順序を再現するシニアエンジニア判断DAG
status: active
view: dev
architecture_ref: docs/architecture/vibepro-senior-engineering-judgment-dag.md
spec_ref: docs/specs/vibepro-senior-engineering-judgment-dag.md
---

# Story: 佐藤の思考順序を再現するシニアエンジニア判断DAG

## 背景

VibeProには、シニアエンジニアが見るべき判断軸をDAGとして表す設計資産があった。しかし、軸の内容が
妥当でも、問題設定を疑う前に分類し、到達していない軸まで一律に集約すると、本来の判断順序と異なる。
その結果、不要な枝の不足証拠が残り、未知がブロックへ変換され、現実の意思決定よりチェックリスト消化が
優先される。

さらに、Story単体の判断だけでは、複数Storyにまたがる「内部機構を追加したが外部成果が改善していない」
という累積傾向を検知できない。そのため、局所的には妥当な改善を並列に積み重ねながら、全体としては
VibeProを肥大化させる可能性が残る。

## ユーザー価値

VibeProを使うシニアエンジニアとして、違和感、ゴール、問題設定、仮説、予測、反証、制約、選択肢を
一つの追跡可能な判断DAGに残し、必要な判断軸だけを適切な順番で評価したい。これにより、自分の思考の
強みを維持しながら、説明力の高い誤仮説や目的による証拠選択の偏りを検出したい。

また、次の並列開発バッチを始める前に、最後に外部成果を確認した時点または簡素化した時点以後の
採用済みStory履歴を一度だけ評価し、`VALUE`、`SIMPLIFY`、`VALIDATE` のいずれで進むかを決めたい。
これにより、個々のPRを直列化せず、成果未確認の追加型改善が次のバッチへ連鎖することを防ぎたい。

## 受け入れ基準

- [x] 判断軸の評価より前に、ゴールと問題設定そのものの妥当性が評価される
- [x] ゴール固定後、問題設定とStory単体判断より前に、因果的な境界以後の採用済みバッチ履歴が評価される
- [x] 履歴、外部成果、現在の問題種別、解決方向を選べる証拠の十分性から `VALUE`、`SIMPLIFY`、`VALIDATE` の開発モードが一つだけ選ばれる
- [x] 検討中バッチの種別や「制約を直接扱う」という自己申告は、開発モード選択に影響しない
- [x] 外部成果が不明な追加履歴は `VALIDATE`、悪化または不変が確認された追加履歴は `SIMPLIFY` へ進む
- [x] 現在の問題が確認済みでも、構造的過剰なら `SIMPLIFY`、解決方向を選ぶ証拠が不足していれば `VALIDATE` へ進む
- [x] 開発モードに反する選択肢は、Story内のリスク評価より前提となる候補から除外される
- [x] 履歴単位はPR数ではなく採用済みバッチであり、一つのバッチ内の並列開発は維持される
- [x] 「3回」などの固定回数ではなく、最後の外部成果または簡素化ベースラインを境界にする
- [x] 問題設定が不適切なら下流の軸を評価せず、再設定を推薦する
- [x] 必要と判断された軸だけが到達可能になり、未到達の軸は全体判断に影響しない
- [x] 各仮説について、予測と、その予測を支持・反証・識別不能とする証拠を追跡できる
- [x] 反証された仮説の枝は閉じ、その枝専用の不足証拠を残さない
- [x] 証拠不足を自動的な合格または失敗にせず、追加調査・安全な先送り・人間判断を区別する
- [x] 影響範囲、可逆性、重大度に応じて軽量判断と深い判断を使い分ける
- [x] 制約違反の選択肢を除外し、残った選択肢と残存リスクから説明可能な推薦を作る
- [x] 一回の判断は非循環で、新しい証拠による更新は親実行を参照する新しい判断として残る
- [x] 判断結果はレビュー可能な助言であり、PR作成、マージ、デプロイを自動で許可または禁止しない

## 成功の見え方

- 同じ入力から同じ到達枝と推薦が得られる
- 推薦から、使われた観測・仮説・証拠・制約へ逆向きに辿れる
- 「なぜこの軸を見なかったか」と「なぜこの仮説を捨てたか」の両方を説明できる
- 次の人間判断または追加調査が、一般論ではなく未解決の仮説に結び付いている
- 次バッチがなぜ価値提供、簡素化、検証のどれになったかを、履歴境界と採用済みバッチへ逆向きに辿れる
