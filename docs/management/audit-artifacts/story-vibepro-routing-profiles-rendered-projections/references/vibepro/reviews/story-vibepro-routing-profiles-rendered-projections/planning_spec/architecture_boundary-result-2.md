# Planning Spec Re-review: architecture_boundary

Status: needs_changes

Metadata and ownership improved, but the task authority was relocated from the current canonical path, profiles are incomplete across kinds, schema-version compatibility is undefined, mirror presence is inconsistent, and regression fixtures miss these surfaces.
