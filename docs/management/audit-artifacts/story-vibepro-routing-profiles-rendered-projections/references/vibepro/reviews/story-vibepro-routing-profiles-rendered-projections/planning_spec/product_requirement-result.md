# Planning Spec Review: product_requirement

Status: needs_changes

The Story intent is valid, but the planning artifacts do not yet define an implementable contract.

- The canonical authority for `artifact_profile` and `feature_slug` is unresolved between Story frontmatter and `.vibepro/config.json`.
- The task machine-model to Markdown projection contract is missing.
- The full lifecycle path-surface matrix is missing.
- Ownership views for Evidence, Test Plan, Gate, and Release are underspecified.
- Fresh-checkout tests currently referenced would pass before this change.

Inspected: Story, Architecture, spec draft, config, artifact routing, spec store, CLI lifecycle consumers, and routing tests.
