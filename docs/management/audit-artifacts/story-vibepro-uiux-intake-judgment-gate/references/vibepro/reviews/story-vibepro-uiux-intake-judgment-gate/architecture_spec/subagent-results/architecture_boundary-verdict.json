{"status":"block","summary":"gate:uiux_intake_judgment's type is omitted from collectUnresolvedRequiredGates' type allowlist, so it never enters blocking_gates/overall_status despite required:true and needs_evidence — the fail-closed contract does not exist yet.","judgment_delta":"Diff looked clean; runtime probe flipped the verdict: enforcement selector does not recognize uiux_intake_judgment_gate, making the gate decorative. Tests only assert the node's own fields, never the enforcement surface.","agent_id":"aef1a2913515a22e1","agent_system":"claude_code","model":"claude-sonnet-5"}
