# e2e_ux review transcript

Agent: 019f305b-b209-7df2-8f45-90918eab36c7
Role: e2e_ux
Status: needs_changes
Reviewed HEAD: f89a0f401488fbbb548ed2b4646e3fd5e253cebf

## Summary

The implementation had strong regression_guard and path_surface_coverage in the focused tests, and the core design-input journey transitions were covered. The remaining blockers were user-facing PR/evidence artifact contradictions:

- PR body final E2E selected a unit artifact because the selector matched "workflow" text in a unit summary.
- Gate DAG reported evidence_reuse hit/passed while the current evidence-reuse artifact reported stale/needs_refresh.

## Evidence

- `node --test test/design-input-judgment.test.js test/e2e/story-vibepro-design-input-judgment-flow.spec.ts` passed 13/13 subtests.
- `node --test --test-name-pattern diagnose-profiles-Next-js-repository test/vibepro-cli.test.js` passed 1/1 subtest.
- `.vibepro/pr/story-vibepro-design-input-judgment/pr-prepare.json` included `pr_context.design_input_judgment` and passed `gate:design_input_judgment` for run `2026-07-04T193725Z`.
- `.vibepro/pr/story-vibepro-design-input-judgment/evidence-reuse.json` correctly marked reuse stale/needs_refresh.
- `.vibepro/pr/story-vibepro-design-input-judgment/pr-body.md` rendered final E2E from `responsibility-authority-current-head-status.json`.
- `.vibepro/pr/story-vibepro-design-input-judgment/gate-dag.json` still reported stale evidence reuse as hit/passed.

## Findings

1. `src/pr-manager.js`: final E2E evidence selection scanned kind/type/summary/command with `/e2e|flow/i`, so non-E2E workflow text could win before the actual E2E command.
2. `.vibepro/pr/story-vibepro-design-input-judgment/gate-dag.json`: stale full-surface artifact contradicted the current `evidence-reuse.json` freshness state.

## Resolution Notes

Both findings were addressed in follow-up commit `8f95e01f34d2618b4964600cc2adb37c413da16b`:

- final E2E evidence now selects exact normalized `kind=e2e` first, then constrained flow/E2E fallbacks without scanning summary text;
- summary-depth PR prepare now removes stale full-surface artifacts, including Gate DAG and related HTML surfaces.
