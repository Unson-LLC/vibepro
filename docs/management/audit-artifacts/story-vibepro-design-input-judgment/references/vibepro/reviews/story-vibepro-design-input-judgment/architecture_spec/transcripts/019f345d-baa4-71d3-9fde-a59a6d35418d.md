STATUS: needs_changes

SUMMARY:
Incomplete/non-accepted waiver visibility is improved, but the accepted-waiver validator still allows a waiver with missing `status` to downgrade an active blocker to `accepted_followup`. That violates the required contract that `decision_id`, `reason`, `artifact`, and `status=accepted` must all be present.

INSPECTION:
Inspected review request, confirmed HEAD `337748aff3504f3f89ef143048f2a22b30b0a4a2`, clean worktree, reviewed `src/pr-manager.js` waiver selection/gate rendering diff and updated CLI/E2E tests. Started focused `node --test` on `test/vibepro-cli.test.js`; interrupted because the large file continued running broad tests despite the pattern.

FINDINGS:
- medium: `/tmp/vibepro-issue245-impl/src/pr-manager.js:7689` `isAcceptedBlockerWaiver` treats `waiver.status == null` as accepted. Because `buildJudgmentAxisGates` uses this predicate to emit `accepted_followup`, a waiver record with `decision_id`, `reason`, and `artifact` but no `status` can still downgrade an `active_blocked` axis. The requested contract requires `status=accepted` to be present.
- low: `/tmp/vibepro-issue245-impl/src/pr-manager.js:7719` `summarizeBlockerWaiverMissingFields` also treats missing status as not missing, so the gate/PR UX would not explain that `status=accepted` is absent. Current tests cover `status: "open"` plus missing `decision_id`, but not the missing-status-only regression.

JUDGMENT_DELTA:
Initial concern was silent downgrade/drop behavior for blocker waivers -> final `needs_changes` because malformed non-accepted records are now attached and visible, but missing `status` still incorrectly qualifies as an accepted blocker waiver and can downgrade a blocker.
