# test_plan / e2e_ux review

Agent: 019f3438-72b5-7450-a6e2-203f638a8cf3
Status: needs_changes

Summary: Incomplete blocker waivers remain blocking, but malformed waiver records are dropped before Gate/PR UX can explain that the waiver was ignored.

Inspection summary: Inspected review request, HEAD/diff, `src/pr-manager.js` waiver/gate paths via graph trace, current `.vibepro/pr/...` artifacts, focused E2E/CLI tests, and ran the accepted/malformed waiver tests.

Judgment delta: initial concern about hidden waiver downgrades -> final needs_changes because valid accepted waivers are visibly `accepted_followup`, but incomplete waiver UX is silent even though it blocks.

Findings:
- `src/pr-manager.js:7701` medium: `findAcceptedBlockerWaiverForSource` filters out waiver decisions missing `decision_id` before attaching them to the axis, so `buildJudgmentAxisGateReason` never reaches its ignored-waiver branch for malformed decision-record waivers. The focused malformed-waiver test asserts blocking behavior, but not user-facing PR/Gate output explaining that an incomplete waiver was ignored.
