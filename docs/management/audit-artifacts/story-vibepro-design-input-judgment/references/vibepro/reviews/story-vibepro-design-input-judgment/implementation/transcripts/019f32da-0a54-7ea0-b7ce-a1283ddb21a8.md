# implementation:runtime_contract review

Agent: 019f32da-0a54-7ea0-b7ce-a1283ddb21a8

```json
{
  "status": "needs_changes",
  "summary": "Design-input diagnosis and gate paths are mostly covered, but PR prepare still emits an unbound pre_implementation_judgment, so the runtime artifact/API contract and path-surface coverage are incomplete.",
  "inspection_summary": "Read the runtime_contract review request, diffed origin/main...HEAD, inspected CLI phase parsing, diagnosis evidence emission, PR prepare gate/context synthesis, next-command surfaces, docs/spec/story artifacts, verification artifacts, and focused tests. Ran focused read-only tests; all passed, but they do not cover the failing contract path.",
  "inspection_evidence": "HEAD a266bbee592ebd0c1a375c50b6d0855cdf4db75c. Focused tests passed: node --test test/design-input-judgment.test.js (7 tests), node --test test/e2e/story-vibepro-design-input-judgment-flow.spec.ts (6 tests), node --test test/responsibility-authority.test.js test/evidence-depth-pr-prepare.test.js (16 tests). Artifact evidence inspected: .vibepro/pr/story-vibepro-design-input-judgment/pr-prepare.json, .vibepro/pr/story-vibepro-design-input-judgment/verification-evidence.json, and .vibepro/diagnostics/*/evidence.json phase summaries.",
  "inspection_inputs": [
    ".vibepro/reviews/story-vibepro-design-input-judgment/implementation/review-request-runtime_contract.md",
    "git rev-parse HEAD",
    "git diff --stat origin/main...HEAD",
    "git diff --name-status origin/main...HEAD",
    "src/cli.js",
    "src/diagnostic-engine.js",
    "src/pr-manager.js",
    "src/architecture-readiness.js",
    "src/pre-spec-readiness.js",
    "src/repo-status.js",
    "src/story-manager.js",
    "test/design-input-judgment.test.js",
    "test/e2e/story-vibepro-design-input-judgment-flow.spec.ts",
    "test/responsibility-authority.test.js",
    "test/evidence-depth-pr-prepare.test.js",
    ".vibepro/pr/story-vibepro-design-input-judgment/pr-prepare.json",
    ".vibepro/pr/story-vibepro-design-input-judgment/verification-evidence.json",
    ".vibepro/diagnostics/*/evidence.json",
    "node --test test/design-input-judgment.test.js",
    "node --test test/e2e/story-vibepro-design-input-judgment-flow.spec.ts",
    "node --test test/responsibility-authority.test.js test/evidence-depth-pr-prepare.test.js"
  ],
  "judgment_delta": [
    "Design-input judgment path is bound to a design_input diagnosis artifact and is surfaced into PR prepare with run_id, artifact_status, gate_status, finding_count, and required actions.",
    "Regression guard evidence exists for the main design-input path and focused tests pass, but the tests do not prove that pre_implementation_judgment remains bound to actual diagnosis evidence.",
    "Path surface coverage is incomplete for the PR prepare/internal synthesis output surface because pre_implementation_judgment is synthesized from PR prepare state instead of reflecting a pre_implementation diagnosis artifact or an explicit missing-artifact state."
  ],
  "findings": [
    {
      "severity": "medium",
      "id": "pre-implementation-pr-context-unbound",
      "detail": "src/pr-manager.js builds pr_context.pre_implementation_judgment with source=pr_prepare, gate_status=null, and no run_id, artifact_status, or finding_count, instead of binding it to a pre_implementation story diagnosis run/evidence or explicitly surfacing an artifact_missing state. The current .vibepro/pr/story-vibepro-design-input-judgment/pr-prepare.json confirms this shape despite existing pre_implementation diagnostics such as .vibepro/diagnostics/2026-07-04T192849Z/evidence.json. Tests at test/design-input-judgment.test.js:224 and test/e2e/story-vibepro-design-input-judgment-flow.spec.ts:189 only assert the phase, so a PR context that loses the actual final consistency evidence still passes. This violates the runtime artifact/API contract and fails path_surface_coverage for the PR prepare/internal synthesis surface."
    }
  ]
}
```
