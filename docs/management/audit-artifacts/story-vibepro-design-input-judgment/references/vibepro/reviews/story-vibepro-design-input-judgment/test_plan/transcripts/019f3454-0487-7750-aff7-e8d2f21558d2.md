# test_plan / e2e_ux review

Agent: 019f3454-0487-7750-aff7-e8d2f21558d2
Status: needs_changes

Summary: Incomplete blocker waivers with missing `status=accepted` can still silently disappear instead of remaining visibly blocking.

Inspection summary: Inspected HEAD diff, `src/pr-manager.js` waiver selection/gate rendering logic, focused CLI/E2E waiver tests, and ran the focused tests successfully.

Judgment delta: initial concern: accepted waivers might be hidden or downgraded incorrectly -> final judgment: partially fixed because complete accepted waivers and missing `decision_id` stay visible, but non-accepted/missing-status waiver records are filtered out before missing metadata can be surfaced.

Findings:
- `src/pr-manager.js:7704` medium: `findBlockerWaiverForSource` only returns decisions with `decision.status === 'accepted'`, while `summarizeBlockerWaiverMissingFields` later claims it can report `status=accepted` as missing/invalid metadata. Because the invalid-status record is discarded before gate construction, that incomplete waiver record is not attached to `axis.blocker_waiver`, so Gate DAG/PR UX cannot visibly explain the missing status metadata and it silently disappears.
