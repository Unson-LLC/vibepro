# architecture_spec / regression_risk review

- agent: 019f2e27-755c-7331-afe5-ec5e605e600d
- head: fb1f14eb27b3029eb11976e9eae60d4ebc553840
- status: needs_changes

## Summary

Current HEAD fb1f14e fixes the prior S004/S005/S006 marker mismatch: Story/Spec now define DIJ-SCENARIO-004/005/006, and the E2E uses S-001..S-006 constants aligned to those clauses. Core regression paths are covered, but path-surface coverage is still incomplete because some user-facing guidance still emits the old diagnosis command without --pre-architecture.

## Inspection Summary

Inspected required files plus adjacent README/CLI/skill/readiness surfaces. Verified HEAD is fb1f14eb27b3029eb11976e9eae60d4ebc553840. Ran and passed test/design-input-judgment.test.js, the E2E flow spec, adjacent readiness tests, and the CLI PR-prepare test. Evidence confirms missing artifact, manifest-only false pass, later pre-implementation preservation, PR gate DAG/context, and CLI first-run next commands are covered; stale docs/readiness remediation remain.

## Judgment Delta

- marker-mismatch concern -> fixed because fb1f14e adds DIJ-SCENARIO-004/005/006 to Story/Spec and the E2E now asserts S-001 through S-006 against the matching scenario clauses; the E2E passed 3/3
- regression/path-surface concern -> needs_changes because implementation and tests cover the core data paths, but README.ja and readiness next-action surfaces still recommend the legacy command without --pre-architecture

## Findings

1. medium: src/architecture-readiness.js:214 Architecture readiness remediation still skips design-input phase.
   The missing-diagnosis next action still says to run `vibepro story diagnose . --id ... --run-graphify` without `--pre-architecture`. Update this remediation to include `--pre-architecture --run-graphify`.

2. medium: src/pre-spec-readiness.js:214 Pre-spec readiness remediation still emits the legacy diagnosis command.
   The pre-spec missing-diagnosis action also omits `--pre-architecture`. Align the command with the new design-input workflow.

3. low: README.ja.md:163 Japanese README Quick Start still shows the old diagnosis command.
   The English README and CLI docs mention `--pre-architecture`, but README.ja Quick Start still instructs `story diagnose ... --run-graphify` without the design-input phase. The same stale command appears again at line 278.
