# gate_evidence review

Status: needs_changes

実装と対象テストは通過したが、判断利用集計の出力経路と未使用artifact判定に不備があり、gate_evidenceとしてpassできない。

## Findings

- high `gate-evidence-path-surface-unpropagated`: bounded summary、usage report、senior-gap synthesisに新しい判断利用集計が伝播していない。
- high `unused-artifact-count-unreachable`: 全entryにdecision_idとconsumer_gateを無条件付与するため、未使用artifact判定が常に0になる。
- medium `spec-gate-provenance-stale`: pre-spec readinessが旧HEADでblocked、architecture checkが未実行。
- low `verification-evidence-observation-weak`: current-head bindingはあるがmachine-readable test artifactがなく、typecheck observationが弱い。

## Inspection

`origin/main...HEAD`、Story/Architecture/Spec、`src/evidence-reuse.js`、usage report、senior-gap synthesis、bounded review artifactsを確認。`node --check src/evidence-reuse.js`、`node --test test/evidence-summary-reuse.test.js`、`node --test test/senior-gap-judgment.test.js`、`git diff --check` はpass。

Initial judgment: 新フィールドと対象テストからpass候補。

Final judgment: 全出力surfaceと非ゼロ未使用ケースが未検証のためneeds_changes。
