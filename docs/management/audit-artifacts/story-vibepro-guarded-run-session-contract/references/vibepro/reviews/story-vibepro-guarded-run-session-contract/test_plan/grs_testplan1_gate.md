# gate_coverage review #1

status: needs_changes

Add a complete initial Run shape fixture; exact corrupt quarantine naming/details; exact repository repair exit-2 byte preservation; explicit internal exit-1 coverage; and executable negative fixtures proving non-ready Gate cannot write `pr_ready` and Run operations cannot dispatch agents/actions, create waivers, merge, or mutate legacy execution outside bootstrap.
