# e2e_ux review #1

status: needs_changes

Add executable CLI coverage for help/discoverability, per-command wrong-root and selection cases, managed restart across Run operations, exact human/JSON success and error rendering, contract exit 2, and unexpected exit 1.
