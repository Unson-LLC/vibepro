# unit_integration review #2

status: needs_changes

1. The complete initial Run fixture references undocumented budget/deadline and initial transition defaults.
2. Dependency-spy and failure-injection tests require a defined ESM-safe dependency-injection seam, or static call-surface assertions for forbidden capabilities.

Restart, migration, Gate transition, CLI matrices, locks, authority, corruption, and mirror recovery are otherwise complete.
