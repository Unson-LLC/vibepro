# Gate Tuning Report — 2026-07

First run of the monthly gate-tuning ritual (see
`docs/guide/gate-tuning-ritual.md`). This is the inaugural pass: it re-runs a stale
diagnosis pack and takes the first real reading of the gate-outcome ROI ledger added
in PR #287 (`feat: add gate outcome ROI ledger`, merged 2026-07-05).

## What was run

```bash
vibepro check oss-readiness . --run-id oss-status-20260706
vibepro usage report . --json
```

Both were run from a fresh worktree at `.claude/worktrees/gate-tuning-loop`
(branch `claude/gate-tuning-loop`, based on `origin/main`).

## oss-readiness: old vs. new

| Run ID | Date | Status | pass | needs_setup | findings |
| --- | --- | --- | --- | --- | --- |
| `oss-status-20260526` | 2026-05-26 | `needs_setup` | 0/5 | 5/5 | 5 (gitleaks, scorecard, syft, grype, reuse all missing) |
| `oss-status-20260706` | 2026-07-06 | `needs_setup` | 4/5 | 1/5 | 1 |

Per-tool detail for the new run:

| Tool | Status | Note |
| --- | --- | --- |
| gitleaks | pass | `gitleaks detect --source . --report-format json --no-banner` — no secret candidates reported |
| syft | pass | 12 SBOM components |
| grype | pass | 0 vulnerability matches |
| reuse | pass | REUSE compliant |
| scorecard | needs_setup | fails before producing JSON: `GitHub token env var is not set` |

The original motivating finding (`oss_readiness.setup.gitleaks.missing`) is
resolved: gitleaks is installed at `/usr/local/bin/gitleaks` and `.gitleaks.toml`
exists at the repo root, and the pack now reports `pass` for it. The pack overall
status is still `needs_setup` because `scorecard` needs a `GITHUB_TOKEN` environment
variable to authenticate against the GitHub API — a distinct, unrelated setup gap
from the one recorded in May. This is a real environment fix (setting a token), not
a Gate/threshold problem, so it is not a candidate for a preset adjustment this
month.

Artifacts:
- `.vibepro/checks/oss-readiness/oss-status-20260526/check.md` (old, canonical repo)
- `.vibepro/checks/oss-readiness/oss-status-20260706/check.md` (new, this worktree)

## Gate-outcome ROI ledger: first read

```text
## Gate Outcome ROI
- entries: 0
Gate別分布: none
降格候補: none
```

The live ledger at `.vibepro/gate-outcomes/ledger.json` is empty in this checkout —
`entry_count: 0`, no distributions, no demotion candidates. PR #287 landed
2026-07-05T18:05:47+09:00 and no story has resolved a required Gate in this checkout
since then, so there is nothing to distribute yet. This is expected for a
same-day-as-merge first read, not a bug.

For context, a pre-merge dogfood run of the same story
(`story-vibepro-gate-outcome-roi-ledger`) is preserved as a canonical audit
artifact at
`docs/management/audit-artifacts/story-vibepro-gate-outcome-roi-ledger/references/vibepro/gate-outcomes/ledger.json`.
It shows 21 resolved-gate entries, of which 20 classified as `unclassified`
(`ambiguous_resolution_surface`) and 1 as `source_fix`. That skew toward
`unclassified` is itself a signal worth watching once the live ledger accumulates
comparable volume — if it persists, the classifier's evidence/rewording heuristics
may need tightening before demotion candidates from `rewording_only_rate` can be
trusted.

## Demotion candidates this month

None. The live ledger has zero entries, and the deterministic demotion rule
(`total_count >= 2 and rewording_only_rate >= 0.6`) has nothing to evaluate against.
No preset or threshold adjustment was made this month.

## Next-month checkpoints

- Re-run `vibepro usage report . --json` and check whether `gate_outcomes.entry_count`
  is still `0`. If stories have started flowing through required Gates, read the
  per-gate distribution for the first time.
- Re-run `vibepro check oss-readiness .` after setting `GITHUB_TOKEN`, to confirm
  `scorecard` moves from `needs_setup` to `pass` and the pack reaches full green.
- If the live ledger accumulates entries, watch the `unclassified` share against the
  pre-merge dogfood baseline (20/21 unclassified) — a similarly high share across a
  larger sample means the classifier needs a source_fix/evidence_added heuristic
  fix before `rewording_only_rate`-based demotion candidates can be acted on safely.
